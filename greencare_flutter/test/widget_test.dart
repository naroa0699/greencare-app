import 'package:flutter_test/flutter_test.dart';

// Test de ejemplo simple incluido en el proyecto. Lo dejo sin tocar,
// sirve solo para comprobar que el entorno de tests arranca.
void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}